@if (country is null)
{
    <Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <FormWithName Label="Country:"
                          Model="country"
                          OnValidSubmit="EditAsync"
                          ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}