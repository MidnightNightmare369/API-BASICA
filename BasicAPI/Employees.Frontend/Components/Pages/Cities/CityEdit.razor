@if (city is null)
{
    <Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <FormWithName Label="City:" Model="city" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}